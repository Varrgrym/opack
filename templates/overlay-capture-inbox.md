# Overlay snippet — capture inbox (satellite)

**Use:** ideas / dumps / sources inbox on a machine that is **not** the product runtime (e.g. Windows satellite vs Linux workhorse).  
**Install profile:** baseline only — see `optional/INSTALL_PROFILES.md`.  
**How:** copy bullets into the project’s `OPERATOR_WORKFLOW.md`; delete what does not fit.  
**NOT:** a second opack · Garden hub execution · library-organizer sprawl.

---

## Standing prompts

- This root is a **capture inbox** — raw thoughts, sources, parse queue — not a product codebase.
- Prefer **Mode B** when organizing; label decided · NOT · parked.
- **Organize ideas here; execute product later** (on the workhorse / hub). Files under product-named folders are reference, not a mandate to build from this machine.
- **Personal ideas must not get lost.** Prefer Keep/Park for media-sparked thoughts, living-systems fragments, creative sketches. Reject only PII / secrets / expired ops noise — not “personal” as a category. See `EXTRACT_AND_ORGANIZE.md`.

## Personality

- Order and notes over chat-only memory; necessary complexity only.

## Behaviors

- Dumps → filter → Keep notes → update a thin map (`ORGANIZED.md` or equivalent); do not invent live credentials.
- **MVP before Gauntlet** when this inbox later feeds a build: brief/DoD/working slice first.
- Bulk Drive/docs: scrape → inbox raw (or off-repo archive) → cluster → operator reads the end result.

## Repeated tasks

- New raw material → `inbox/` or `sources/` first; parse before promoting to by-family (or equivalent).
- Work that needs product runtime → queue file for the workhorse; do not fight hub/plant execution here.

## Refuses / pins (this repo)

- Do not paste Garden Immune, opack lab essays wholesale, or foreign product constitutions into this inbox.
- Do not turn this into a monorepo of all projects.
- Never commit raw secret-bearing exports, OAuth tokens, or unredacted dumps.

## Open experiments

-
